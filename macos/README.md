# Install
## Neovim, Tmux
brew install neovim
brew install tmux
brew install alacritty
brew install fzf

## Ripgrep
fzf needs `rp` or `ag`
```
brew install ripgrep
```

## Fonts
https://github.com/ryanoasis/nerd-fonts
